services:
  app:
    image: ${APP_IMAGE:-pawonwarga-be:latest}
    container_name: pawonwarga-app
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file: .env
    networks:
      - internal

networks:
  internal:
    driver: bridge
